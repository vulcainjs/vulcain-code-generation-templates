{
  "transform": {
    "replace" : [
      { "filter" : "package.json"},
      { "filter" : "Dockerfile" },
      { "filter" : ".vscode/launch.json" },
      { "filter" : "src/index.ts" }
	   ],
    "rename": [
    ]
   },
   "scripts": [
        "npm install"
    ]
}