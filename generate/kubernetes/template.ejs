apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    serviceName: <%= this.state.service %>
    serviceVersion: "<%= this.state.version %>"
  name: <%= this.state.fullServiceName %>
spec:
  replicas: 1
  selector:
    matchLabels:
      serviceName: <%= this.state.service %>
      serviceVersion: "<%= this.state.version %>"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        serviceName: <%= this.state.service %>
        serviceVersion: "<%= this.state.version %>"
    spec:
      containers:
        - image: <%= this.state.image %>
          name: <%= this.state.service %>
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            timeoutSeconds: 1
            periodSeconds: 10
            failureThreshold: 1
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP

---
kind: Service
apiVersion: v1
metadata:
  name: <%= this.state.fullServiceName %>
spec:
  ports:
    # Accept traffic sent to port 80
    - name: http
      port: 8080
  selector:
      serviceName: <%= this.state.service %>
      serviceVersion: "<%= this.state.version %>"
  type: NodePort